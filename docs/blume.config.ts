import { defineConfig } from "blume";

export default defineConfig({
  title: "Grund UI",
  description: "Nativa HTML-komponenter för befintliga CMS-sajter. En CSS-länk plus kopierad markup.",
  content: {
    root: "content",
  },
});
